# formatConverterCList
Converts provided file to a given dictionary format. Specific use. But can be modified. 
# Python 2.7 
# To run (input script,filename = argv) launch from python as ex. fileName.py fileToBeConverted.txt
